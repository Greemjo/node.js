class Airport {
  constructor({ name, country, region, img_url }) {
    this.name = name;
    this.country = country;
    this.region = region;
    this.img_url = img_url;
  }
}

module.exports = Airport;
